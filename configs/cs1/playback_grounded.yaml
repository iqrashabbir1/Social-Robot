experiment_name: playback_grounded
case_study: CS1
seed: 42
inputs:
  ros2_workspace: ros2_ws
  replay_source: outputs/logs/physiology_ros2_bridge.jsonl
outputs:
  csv_dir: outputs/csv/cs1/playback_grounded
  figure_dir: outputs/figures/cs1/playback_grounded
preprocessing:
  timestamp_unit: ms
evaluation:
  metrics:
    - end_to_end_latency
    - synchronization_error
    - event_timing
  data_source_type: mixed
  runtime_type: ros2_playback_grounded
  model_status: fully_runnable
  evidence_level: framework_validation
plot:
  save_png: true
  save_svg: true
simulation:
  steps: 84
fault_injection:
  enabled: false
