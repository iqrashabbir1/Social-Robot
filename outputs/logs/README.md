# Logs Workspace

Store benchmark run logs, ROS2 replay traces, dashboard event logs, and simulator execution summaries here.

This directory is intentionally treated as local-only workspace output.
Tracked research artifacts live under `outputs/csv/`, `outputs/tables/`, and `outputs/figures/`.
